import { Box } from "@material-ui/core";

const Trip = () => {
  return (
    <Box display="flex">
      <div id="pac-container">
        Here are the trips you searched for:
      </div>
    </Box>
  );
};

export default Trip;
